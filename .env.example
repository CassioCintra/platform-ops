# ── PostgreSQL ──────────────────────────────
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=order_platform
POSTGRES_USER=order_user
POSTGRES_PASSWORD=change_me

# ── Kafka ────────────────────────────────────
KAFKA_BROKER=kafka:9092
KAFKA_CLUSTER_ID=order-platform-cluster

# ── Keycloak ────────────────────────────────
KEYCLOAK_PORT=8080
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=change_me
KEYCLOAK_DB=keycloak
KEYCLOAK_DB_USER=keycloak_user
KEYCLOAK_DB_PASSWORD=change_me
# Note: KEYCLOAK_ADMIN and KEYCLOAK_ADMIN_PASSWORD map to
# KC_BOOTSTRAP_ADMIN_USERNAME and KC_BOOTSTRAP_ADMIN_PASSWORD in docker-compose.yml

# ── order-service ───────────────────────────
ORDER_SERVICE_PORT=8081

# ── dashboard-service ───────────────────────
DASHBOARD_SERVICE_PORT=8082
